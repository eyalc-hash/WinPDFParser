{
  "name": "pdf-parser-monorepo",
  "version": "0.1.0",
  "private": true,
  "description": "Local-first Windows desktop app that OCRs PDFs, AI-renames them, and provides full-text search.",
  "workspaces": [
    "app"
  ],
  "scripts": {
    "lint": "npm run lint --workspaces --if-present",
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
